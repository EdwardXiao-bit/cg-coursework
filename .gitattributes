# ---- 行尾策略 ----
# Windows 脚本必须用 CRLF，否则 cmd.exe 解析批处理可能出错
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf

# 二进制文件：不做行尾转换，也不做文本差异
*.docx  binary
*.doc   binary
*.mp4   binary
*.png   binary
*.jpg   binary
*.zip   binary
